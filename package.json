{
  "name": "trim-obj",
  "version": "0.0.1",
  "description": "Recursively trim object values",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "prepublish": "yarn run test & yarn run build"
  },
  "dependencies": {
    "map-obj": "^4.3.0"
  },
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "jest": "^27.3.1",
    "prettier": "^2.5.0",
    "ts-jest": "^27.0.7",
    "tsc": "^2.0.3",
    "typescript": "^4.5.2"
  }
}
